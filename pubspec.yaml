name: world_cup
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
    
  flutter_bloc: ^9.1.1
  
  drift: ^2.34.0
  path_provider: ^2.1.6
  path: ^1.9.1

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  drift_dev: ^2.34.0
  build_runner: ^2.15.0


  flutter_lints: ^6.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/imagens/